# エージェント指示

## 指示の適用

- システム、開発者、ユーザーからの指示を最優先する。
- 作業開始時に、作業対象のプロジェクトにある追加の指示ファイルを確認する。上位の指示と矛盾しない範囲では、プロジェクト固有のルールを優先する。
- このファイルには、どのプロジェクトにも共通する短く検証可能なルールだけを置く。技術スタック、ビルド方法、リポジトリ固有の事情はプロジェクト側の指示に置く。

## 永続作業メモ

- 継続作業に必要な非機密の事実、判断、現在の状態は、作業ディレクトリの `working-memory/` に記録する。
- 意味のある作業を始める前に `working-memory/INDEX.md` を読み、必要に応じて関連ファイルも読む。
- `working-memory/` または `INDEX.md` がなく、継続作業に必要かつ書き込み可能な場合は、最小構成を作成する。読み取り専用などで更新できない場合は、本来の作業を止めず、必要なら最後に簡潔に伝える。
- 簡単な質問や一度きりの回答は記録不要とする。実装、調査結果、重要な判断、複数ターンにまたがる作業は、完了時に適切なメモを更新する。
- `AGENTS.md` は短く保つ。事実、作業ログ、長い文脈は `working-memory/` に置く。
- 秘密情報、API キー、パスワード、非公開トークン、慎重に扱うべき個人情報は作業メモに保存しない。
- 作業メモは、ユーザーの明示的な依頼、またはプロジェクト側での管理方針がない限り、コミットに含めない。

## 更新ルール

- 時系列の作業記録は `working-memory/worklog/YYYY-MM.md` に追記する。
- 長期的な好みや繰り返し使う進め方は `working-memory/profile.md` に追記する。
- 現在進行中の状態と次の作業は `working-memory/current.md` に記録する。
- 長く影響する判断と理由は `working-memory/decisions.md` に記録する。
- 古くなった情報は黙って削除せず、「古い」「置き換え済み」と分かる形で残す。

## 変更と検証

- 変更前に対象ファイルと既存の未コミット変更を確認し、依頼と無関係な変更は保持する。
- 削除、上書き、外部への送信など、取り消しにくい操作は、明示的な依頼または承認なしに行わない。
- 変更後は、利用可能な範囲で最も関連性の高い検証を実行する。実行できない場合は、その理由と未検証の範囲を伝える。
- 完了時は、変更内容、検証結果、残る制約を簡潔に伝える。
