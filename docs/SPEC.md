# SOC Workflow — 仕様書

作成日: 2026-09-01
状態: **ドラフト（提案用モックで検証中）**
前提資料: [`../PROJECT-BRIEF.md`](../PROJECT-BRIEF.md)
モック: [`../mock/soc-flow-editor-mock.html`](../mock/soc-flow-editor-mock.html)（エディタ＋試走＋書き出し）
　　　　[`../mock/soc-flow-mock.html`](../mock/soc-flow-mock.html)（初期の閲覧専用モック）

---

## 0. この文書の位置づけ

既存モックは「作られたフローを**辿る**」ツールだった。
本仕様は、そこに「フローを**作る**」機能を加えたアプリケーションを定義する。

主目的は **どのようなフローにするかを検討すること**。本番運用は副次的な目標とする。
この優先順位が、以下の判断の根拠になっている。

### 提案用モックとの関係

本仕様を提案・検証するため、`mock/soc-flow-editor-mock.html` を作成した。
**バニラ HTML + CSS + JavaScript の単一ファイル**で、ビルド不要・依存ゼロ。

| | 採用 | 理由 |
| --- | --- | --- |
| 提案用モック | バニラ HTML/CSS/JS | 提案の場でダブルクリックして開ける。書き出し HTML はどのみちバニラなので、そのコードがテンプレートの原型になる |
| 本体アプリ | React + TypeScript | 状態管理と型の恩恵が効くのは本体から。単一画面のモックではビルド環境のコストが上回る |

モックは仕様を固めるための道具であり、本体アプリの実装基盤ではない。
ただし **viewer（試走・書き出し）のコードは本体でもそのまま使える**形にしてある（7 章）。

本文中の 🔵 は、モックで実際に動かして確定した項目を示す。

---

## 1. 何を作るのか

SOC のインシデント対応フローを、**作り・試し・配る**ためのアプリケーション。

対応者が使う「手順書」そのものではなく、**手順書を設計するための道具**である。
設計した結果を対応者に届ける手段として、単一 HTML の書き出しを持つ。

### 中心となる 3 つの操作

| | 操作 | 画面上の主役 |
| --- | --- | --- |
| ① | 事象を作成する（例：ランサムウェア感染検知） | 事象リスト・ヘッダー |
| ② | 対応項目（タスク）を選び、組み合わせる | タスクパレット → フローキャンバス |
| ③ | 各項目で何をするかを登録する | インスペクタ |

この 3 つが、画面の 3 つのペインにそのまま対応する。

---

## 2. 技術構成と、その理由

| 層 | 採用 |
| --- | --- |
| フロントエンド | React |
| バックエンド | FastAPI |
| データストア | SQLite（ファイル 1 つ） |
| 配布物 | 単一 HTML（データ埋め込み・依存ゼロ） |

### 「単一 HTML・外部依存ゼロ」の扱い

`PROJECT-BRIEF.md` の設計判断「単一 HTML・外部依存ゼロ」は、
SOC 環境のネットワーク制限と、配布がファイルコピーだけで済むことを理由にしていた。

本仕様では、この判断を **配布物側に引き継ぐ** ことで両立させる。

```
┌─────────────────────────┐          ┌──────────────────────┐
│ エディタ（React+FastAPI）│  書き出し │ 単一HTML             │
│ フローを設計する人が使う │ ───────→ │ 対応者が辿る         │
│ DBでタスクを部品化・再利用│          │ 依存ゼロ・ファイルコピー│
└─────────────────────────┘          └──────────────────────┘
```

**検討環境はリッチに、配布物は軽量に。**

DB を持つことで、既存モックでは実現できなかった以下が可能になる。

- タスクを**部品として育てる**（どの事象で何回使われているかが見える）
- 事象をまたいだタスクの一括改訂
- 案 A / 案 B の比較（フェーズ 2）

### 利用形態

**ローカル単独利用**を前提とする。認証・権限・同時編集の考慮は行わない。
ただしデータ模型と API は、将来の多人数利用で破綻しない形にする（3 章・8 章）。

共有は JSON / HTML ファイルの受け渡しで行う。

---

## 3. データモデル

既存モックの `PHASES` / `TASKS` / `EVENTS` を正規化する。

```
phase ──< task ──< step >── event
                    │
                    ├──< step_condition
                    │
                    └──1:0..1── decision ──< decision_option
```

### phase — フロー図の列

| 列 | 型 | 内容 |
| --- | --- | --- |
| `id` | int | PK |
| `key` | text | 一意キー（例 `recv`） |
| `name` | text | 表示名（例 `受信・確認`） |
| `color` | text | 列の色 |
| `sort_order` | int | 左からの並び |

組織の用語に合わせて画面から編集できる（`PROJECT-BRIEF.md` 6 章の優先度：高）。

### task — フロー図のボックス（＝再利用される部品）

| 列 | 型 | 内容 |
| --- | --- | --- |
| `id` | int | PK |
| `key` | text | 一意キー（例 `iso`） |
| `phase_id` | int | 所属する列 |
| `label` | text | ボックスに表示する名称 |
| `note` | text | 補足 |

**タスクは事象に属さない。** 「端末の隔離」は EDR 検知でもランサムウェアでも使う。
この再利用性が、DB を持つ最大の理由である。

### event — 事象（＝フロー 1 本）

| 列 | 型 | 内容 |
| --- | --- | --- |
| `id` | int | PK |
| `key` | text | 一意キー |
| `title` | text | 事象名 |
| `sub` | text | 補足説明 |
| `severity` | text | `S1` / `S2` / `S3` |
| `created_at` / `updated_at` | datetime | |

### step — ある事象における 1 つの作業指示

| 列 | 型 | 内容 |
| --- | --- | --- |
| `id` | int | PK |
| `event_id` | int | 所属する事象 |
| `task_id` | int | 参照するタスク（＝どのボックスを光らせるか） |
| `sort_order` | int | 実施順 |
| `title` | text | 手順のタイトル |
| `detail` | text | 手順の詳細（リッチテキスト） |
| `sla` | text | SLA バッジ（例 `15分`, `即時`）。任意 |
| `escalate` | bool | エスカレーション判断が必要 |

> 🔵 **フロー図のボックスは、タスクではなく手順に 1 対 1 で対応させる。**
> 既存モックはボックスをタスク単位で描いていたため、1 つの事象で同じタスクを 2 回使うと
> ボックスが 1 つに融合し、経路が壊れていた（ダミーデータの `login`・`phish` が該当）。
> 手順ごとにボックスを持たせることで解消する。
> 「端末の隔離」を復旧の前後で 2 回実施するようなフローも、そのまま描ける。

### decision — 判断ステップ（既存 `ask`）

| 列 | 型 | 内容 |
| --- | --- | --- |
| `id` | int | PK |
| `step_id` | int | この手順が判断を尋ねる |
| `key` | text | 回答の保存先キー（例 `tp`） |
| `label` | text | 質問文 |

**decision_option**: `id` / `decision_id` / `value` / `label` / `sort_order`

### step_condition — 表示条件（既存 `when`）

| 列 | 型 | 内容 |
| --- | --- | --- |
| `id` | int | PK |
| `step_id` | int | 条件付きで表示される手順 |
| `decision_key` | text | 参照する判断のキー |
| `value` | text | この値のときだけ表示する |

**1 手順に複数行を持てる形にし、AND で結合する。**

既存モックの `when:"tp=yes"` は単一条件しか表現できず、
`PROJECT-BRIEF.md` 5 章で「判断のネスト未対応」として課題になっていた。
テーブルを 1:N にしておくことで、**データ構造の側で先回りする**。

> 🔵 当初は「MVP は単一条件のみ」としていたが、モックで実装したところ
> **UI コストがほぼ変わらなかった**ため、複数条件（AND）を MVP に含める方針に変更した。
> 未回答の条件が残っている手順は「まだ分からない」として表示したままにする
> （回答前は分岐の存在を見せる、という設計判断と整合する）。

### 将来のための予約（フェーズ 2、MVP では作らない）

- `flow_link` — ある手順から別の事象へ遷移する（「フィッシング → 不審ログインへ移行」）
- `event_snapshot` — 検討中の案を凍結して比較する

---

## 4. 画面仕様

### 4.0 モード構成

**1 画面でモードを切り替える。**

| モード | 役割 |
| --- | --- |
| **編集** | フローを組み立てる |
| **試走** | 対応者として辿る。分岐に答え、条件外の手順が消えるのを確認する |

検討ツールとしては、**この往復そのものが本体**である。
作った直後に辿って、分岐の消え方を見て、また直す。切り替えは 1 クリック。

試走モードで付けた進捗・回答は、編集モードに戻ると破棄される（下書きを汚さない）。

---

### 4.1 編集モードのレイアウト

```
┌────────────────────────────────────────────────────────────────┐
│ ヘッダー  事象名 / 重大度 / [ 編集 | 試走 ] / 書き出し          │
├──────────┬──────────────────────────────┬─────────────────────┤
│ タスク   │ フローキャンバス             │ インスペクタ        │
│ パレット │                              │                     │
│          │ フェーズ列 × タスクボックス  │ 選択中の手順を編集  │
│ 検索     │ ドラッグで投入               │                     │
│ フェーズ別│ クリックで選択              │ タイトル / 詳細     │
│ 使用頻度 │ 接続線が経路を示す           │ SLA / エスカレ      │
│ ＋新規   │                              │ 表示条件 / 判断     │
│          │                              │                     │
│    ②     │              ②               │          ③          │
├──────────┴──────────────────────────────┴─────────────────────┤
│ 手順レーン   ①→②→③… 並べ替え・分岐の見取り図                  │
└────────────────────────────────────────────────────────────────┘
```

3 ペイン ＋ 下部レーン。左から右へ「選ぶ → 置く → 書く」の順に並ぶ。

---

### 4.2 タスクパレット（左）— 操作②の入口

タスクを**部品カタログ**として見せる。

| 要素 | 内容 |
| --- | --- |
| 検索ボックス | ラベル・補足を横断検索 |
| フェーズ別グループ | 列の色で塗り分け、折りたためる |
| 使用中バッジ | この事象で既に使っているタスクに印 |
| **使用回数** | 他の事象を含めて何回使われているか（例 `12 事象で使用`） |
| ＋新規タスク | フェーズを選んで作る。作成後そのままパレットに並ぶ |

**使用回数を出す理由**: 検討中に「これは組織の定番手順か、この事象だけの例外か」が
一目で分かる。部品として育っているタスクと、一度きりのタスクを区別できる。

---

### 4.3 フローキャンバス（中央）— 操作②の本体

- 列 = フェーズ。ボックス = 投入されたタスク。
- **ドロップすると、タスク定義のフェーズ列に自動配置される。**
  作る側は「何をやるか」だけを考えればよく、位置は考えない。
- ドラッグ中は投入先の列が発光し、着地位置にゴーストが出る。
- 順序は「投入した順」が既定。並べ替えは下部レーンで行う（4.5）。
- ボックスをクリックで選択 → インスペクタに内容が出る。
- 同じタスクを 2 回投入できる（「再スキャン」を復旧前後で 2 回など）。

---

### 4.4 インスペクタ（右）— 操作③

選択中の手順に「何をするか」を書く場所。

| 項目 | 入力 |
| --- | --- |
| 手順のタイトル | 1 行 |
| 詳細 | リッチテキスト。強調（既存の `<code>` 相当）を入れられる |
| SLA | 任意（`15分` / `即時` など） |
| エスカレーション | チェック |
| **表示条件** | 「この手順を出す条件」。既存の判断から選ぶプルダウン形式 |
| **判断を設定** | この手順を判断ステップにする。質問文と選択肢を登録 |

#### 表示条件の見せ方

Excel の「〇〇なら□□へ飛ぶ」を、対応者が飛び先を探さない形にするのがこのツールの中核。
**編集する側にも、飛び先を書かせない。**

```
表示条件   [ 常に表示 ▾ ]
           ├ 常に表示
           ├ 「判定結果はどちらですか？」が … [ 真陽性 ▾ ]
           └ 「健全なバックアップは存在しますか？」が … [ 存在しない ▾ ]
```

- プルダウンには、**この手順より前にある判断ステップだけ**が並ぶ。
  後ろの判断は選べない（未来の答えに依存する矛盾を構造的に防ぐ）。
- 🔵 条件は複数指定でき、AND で結合する。前方に判断が無い手順では
  「＋条件を追加」を無効化し、理由を文言で示す。
- 🔵 並べ替えで手順が判断より前に移動した場合、条件は消さずに残し、**検証でエラーとして報告する**。
  黙って条件を消すと、作った人の意図が失われるため。

#### 判断ステップの制約

- 判断ステップには完了ボタンを出さない（`PROJECT-BRIEF.md` 4 章の設計判断を継承）。
- 選択肢は 2 つ以上。
- 判断を削除しようとしたとき、それを参照している手順があれば**削除前に一覧で警告する**。

---

### 4.5 手順レーン（下部）— 順序と分岐の見取り図

キャンバスが「どの列で何をやるか」を見せるのに対し、レーンは「**どの順でやるか**」を見せる。

- 手順が番号付きで横に並ぶ。ドラッグで並べ替え。
- 判断ステップには分岐マーク。
- その判断に依存する手順は、**選択肢ごとの色で束ねて表示する**。
  「真陽性ならこの 5 つ、誤検知ならこの 3 つ」が並びの上で見える。
- 並べ替えで表示条件が矛盾する位置（判断より前に移動）に来たら、その場で警告する。

---

### 4.6 試走モード

既存モックの体験を踏襲する。以下は `PROJECT-BRIEF.md` 4 章の設計判断をそのまま継承する。

- 現在ステップを 3 か所（ヘッダー・フロー図・手順リスト）で同時に示す
- 状態は色だけに頼らず ▶ / ✓ / — の記号を併用する
- 現在ステップにだけ大きな完了ボタンを出す
- 判断ステップは選択肢のみ（完了ボタンなし）
- 回答前はすべての手順を表示し、分岐の存在を見せる
- 条件外になった手順は取り消し線＋淡色にし、進捗の分母から外す

#### 検討ツールとして追加するもの

| 機能 | なぜ要るか |
| --- | --- |
| **経路サマリ** | この回答での手順数と SLA 合計を出す。「この分岐だと何分かかるフローなのか」が設計中に分かる |
| **経路一覧**（フェーズ 1） | 全分岐の組み合わせを列挙し、手順数・所要時間を並べて比較する。**どの経路が長すぎるかを設計段階で見つける** |
| 到達不能の検出 | どの回答でも表示されない手順を警告する。条件の書き間違いを拾う |

経路一覧は既存モックにない、**検討ツール固有の機能**である。
対応者は 1 本しか辿らないが、設計者は全部を見る必要がある。

---

## 5. ビジュアル方針 —「無駄にかっこよく」を仕様に落とす

「かっこよく」のままでは実装できないので、原則と具体に分ける。

### 原則

1. **演出は状態の変化を伝えるときだけ使う。** 常時動き続けるものは置かない。
   SOC は長時間画面を見続ける。動き続ける装飾は疲労になる。
2. **色は情報を補強するもので、情報そのものではない。**
   記号（▶ / ✓ / —）と線種を併用する。既存の設計判断を継承する。
   🔵 ただしこれは「色を使わない」という意味ではない。フェーズ色は常に見えるようにする（下記）。
3. `prefers-reduced-motion` を尊重し、指定時はすべての遷移を無効化する。
4. ダークテーマを基調とする（SOC は暗い室内・長時間運用が多い）。

### 🔵 フェーズ（カテゴリ）の色

**受信・確認 / 情報収集 / 判断 / 封じ込め / 復旧 / 記録・報告 に、それぞれ固有の色を割り当てる。**
その色を、画面のすべての場所で一貫して使う。

| 場所 | 色の出し方 |
| --- | --- |
| キャンバスの列見出し | 見出し文字と下線 |
| タスクボックス | 左端 4px のバー |
| タスクパレット | グループ見出しの色ドットと文字、カードの左端バー |
| 手順レーンのチップ | 上端 3px のバー |
| 手順リストの所属表示 | 行頭の色ドット |
| インスペクタのタスク参照 | 左端バー |

**フェーズ色は状態表示に上書きされない。**
既存モックはボックスの左バーを状態色（現在＝黄／完了＝緑）で塗り替えていたため、
選択した瞬間にカテゴリが分からなくなっていた。
状態は **枠線・地色・記号（▶ / ✓ / —）** で示し、左バーはフェーズ色のまま残す。
これにより「今どの段階の作業をしているか」と「その手順の状態」が同時に読める。

分岐の選択肢にも色を割り当てる（判断ごとに別系統）。
手順レーンでは、その色で条件付き手順を束ねて表示する。

### 具体

| 場面 | 演出 |
| --- | --- |
| パレットをドラッグ | カードが持ち上がる。投入先のフェーズ列が発光し、他の列が沈む |
| タスクを投入 | ボックスがスケールインし、接続線が始点から終点へ**描き伸びる** |
| ボックスを選択 | 輪郭が光り、インスペクタが右からスライドイン |
| 分岐を設定 | 判断ステップから下流が枝分かれし、選択肢ごとに色が付く |
| 試走で分岐に回答 | 選ばれなかった枝が灰に落ちてフェードする（消さずに残す） |
| 現在ステップ | ゆっくりしたパルス（1 か所だけ。原則 1 の例外として明示する） |
| 手順を完了 | 接続線に緑のグラデーションが一度だけ流れる |
| SLA 超過 | バッジが警告色に変わる。点滅はしない |

### タイポグラフィ・フォント

Windows は `Meiryo UI`、macOS は `Hiragino Sans`（既存モックを継承）。
書き出し HTML でも同じスタックを使う。

---

## 6. 接続線の要件

`PROJECT-BRIEF.md` に記録されている通り、既存モックの接続線ロジックには
**「後戻りが 5 本を超えると下部の迂回路が混む」という既知の限界がある。**
そのため本仕様では、**実装手段ではなく求める振る舞いを要件として書く。**

| ID | 要件 |
| --- | --- |
| R1 | 前進する線は、ボックスの**右辺から出て左辺に入る**。視線の流れを一定にする |
| R2 | 後戻りの線は、他のボックスを横切らない |
| R3 | 複数の線が重ならない。同じ領域を通る線はレーンを分ける |
| R4 | 完了 / 未了 / 対象外が、色以外（線種・太さ）でも判別できる |
| R5 | 任意の線について、出発点と到着点が目で追える |
| R6 | 後戻りが一定数を超えたら**警告を出す**。線を無理に整えるより、フローの並び順を疑わせる |

> 🔵 R4 の具体: 完了＝緑の実線 / 未確定（回答待ちの分岐をまたぐ）＝灰の破線 / 通常＝黄の実線。
> 線の中点に通し番号を置き、R5 を補助する。
> 🔵 R6 の具体: しきい値は 5 本。**モーダルの検証結果だけでなく、
> キャンバスの見出しに常時表示する**（「後戻りが 7 本 — 並び順を見直してください」）。
> 作っている最中に気づけないと、直す動機にならないため。

R6 は既存ブリーフの指摘（「線がごちゃつくのは、フロー自体が行ったり来たりしているサイン」）を
機能として実装するもの。**可視化の限界を、検討の材料に変える。**

### 実装手段の判断（🔵 決定済み）

| 案 | 利点 | 欠点 |
| --- | --- | --- |
| **自前 SVG**（既存ロジックを移植・改良） | 見た目と規則を完全に制御できる。書き出し HTML とコードを共有できる | レイアウト計算を自分で持つ。既存ロジックの課題（R3・R6）を解く必要がある |
| **React Flow** | ドラッグ・ズーム・ミニマップが最初から使える。実装が早い | 接続線の規則がライブラリ依存になる。書き出し HTML 側は別実装になる |

🔵 **判断結果: 自前 SVG を採用する。** 提案用モックで 5 事象すべてを描画し、R1〜R6 を満たすことを確認した。
詳細と残る懸念は 11 章に記す。React Flow は採用しない
（接続線の規則を完全に制御でき、書き出し HTML とコードを共有できる利点が上回ったため）。

---

## 7. 書き出し

### 単一 HTML

- データを JSON として埋め込み、1 ファイルとして出力する。
- **編集機能は含めない。** 対応者に配るのは手順書であって、編集ツールではない。
- 出力単位は事象 1 件 / 複数件を選べる。事象 1 件のときは左の事象リストを省く。
- 外部依存ゼロ・ネットワーク接続不要を維持する。

#### 🔵 実現方式（モックで検証済み）

**試走モードと書き出し HTML で、同じ CSS と同じ JavaScript を使う。**

viewer の CSS と JS を専用のブロックに分けて置き、
書き出し時はその `textContent` をそのままテンプレートに埋め込む。

```
<style id="viewerCss">  … 試走モードも書き出しHTMLも、これを使う
<script id="viewerJs">  … mountViewer(root, DATA, opt) を定義
        ↓ 書き出し
単一HTML = viewerCss + データJSON + viewerJs + mountViewer 呼び出し
```

- viewer のコードは外部の変数を一切参照しない（この制約さえ守れば埋め込みが成立する）。
- **書き出し HTML が「絵」ではなく本物になる。** 試走で見えているものが、そのまま配布物になる。
- 本体アプリ（React）でも、この viewer は独立した 1 ファイルとして再利用できる。
- 埋め込む JSON 内の `<` は `<` にエスケープする（`</script>` による早期終了を防ぐ）。

実測サイズ（ダミーデータ、圧縮なし）: 事象 1 件で約 31 KB、全 5 事象で約 43 KB。
外部参照ゼロを確認済み。

### JSON

- 事象・タスク・フェーズを含む定義ファイル。
- インポートで別環境に取り込める。差分レビュー・バックアップに使う。
- **これが検討結果の正本**とする。

---

## 8. API 設計（FastAPI）

ローカル単独利用のため認証は持たない。ただしリソース設計は多人数利用に耐える形にする。

```
GET    /api/phases                   一覧
POST   /api/phases                   作成
PUT    /api/phases/{id}              更新
DELETE /api/phases/{id}              削除（使用中なら 409）
PUT    /api/phases/order             並べ替え

GET    /api/tasks                    一覧（?phase_id= / ?q= で絞り込み）
GET    /api/tasks/{id}/usage         どの事象で使われているか
POST   /api/tasks                    作成
PUT    /api/tasks/{id}               更新
DELETE /api/tasks/{id}               削除（使用中なら 409 ＋ 使用箇所を返す）

GET    /api/events                   一覧
GET    /api/events/{id}              1 件（steps / decisions / conditions を含む）
POST   /api/events                   作成
PUT    /api/events/{id}              更新
DELETE /api/events/{id}              削除
POST   /api/events/{id}/duplicate    複製（案の派生に使う）

POST   /api/events/{id}/steps        手順を追加
PUT    /api/steps/{id}               手順を更新
DELETE /api/steps/{id}               手順を削除
PUT    /api/events/{id}/steps/order  並べ替え

GET    /api/events/{id}/paths        全分岐経路の列挙（経路一覧用）
GET    /api/events/{id}/validate     到達不能・条件矛盾の検査

GET    /api/events/{id}/export/json
GET    /api/events/{id}/export/html
POST   /api/import
```

### 削除の扱い

タスク・フェーズ・判断の削除は、参照している側を壊す。
**削除前に必ず参照箇所を返し、確認させる。** 黙って消さない。

---

## 9. 非機能・制約

| 項目 | 内容 |
| --- | --- |
| 起動 | ローカル。バックエンド（uvicorn）とフロントエンドの開発サーバ |
| 認証 | なし（ローカル単独利用） |
| データ | SQLite ファイル 1 つ。バックアップはファイルコピー |
| 外部送信 | **行わない。** 入力内容をネットワークに出さない |
| ブラウザ | 現行の Chromium 系 / Firefox |

### 機密性（`PROJECT-BRIEF.md` 7 章を継承）

対応フローは「**どこを見ていて、どこを見ていないか**」の情報を含む。

- ダミーと実データを同じ DB ファイルに混ぜない。
- 実データを入れた DB ファイル・書き出し HTML は、通常の資料より慎重に扱う。
  社外共有・クラウド保存・スクリーンショットに注意する。
- 個人名・IP・ホスト名・実際の検知ルール名を含めるかは、方針を決めてから投入する。

---

## 10. 段階

### フェーズ 0 — MVP

「事象を作り、タスクを組み合わせ、内容を書き、辿り、配る」が一周する。

- [ ] フェーズ・タスク・事象の CRUD
- [ ] タスクパレット（検索・フェーズ別・新規作成）
- [ ] キャンバスへのドラッグ投入・自動配置
- [ ] インスペクタ（タイトル・詳細・SLA・エスカレーション）
- [ ] 手順レーンでの並べ替え
- [ ] 判断ステップの登録
- [ ] 表示条件（**複合条件・AND**）🔵 モックで前倒し
- [ ] 試走モード
- [ ] 単一 HTML 書き出し
- [ ] 接続線 R1〜R6
- [ ] 検証（条件の前後関係・到達不能・後戻り過多）🔵 モックで前倒し
- [ ] 経路一覧 🔵 モックで前倒し
- [ ] タスク使用回数の表示 🔵 モックで前倒し

> 🔵 モックで実装したところ、当初フェーズ 1 に置いていた 4 項目は
> いずれも実装コストが小さく、かつ**検討ツールとしての価値が大きい**ことが分かったため
> フェーズ 0 に繰り上げた。特に経路一覧と検証は、
> 「作った本人が気づけないフローの欠陥」を拾うため、あとから足すより最初から在るべきものだった。

### フェーズ 1

- [ ] JSON 入出力
- [ ] 詳細フィールドのリッチテキスト編集（現状は生テキスト入力）
- [ ] フェーズの追加・削除・並べ替え UI
- [ ] タスクの改名・削除（参照箇所の警告つき）
- [ ] 経路の比較ビュー（2 経路を並べる）

### フェーズ 2

- [ ] フロー間リンク
- [ ] スナップショット・案の比較
- [ ] 既存 Excel からの取り込み
- [ ] チーム共有（認証・同時編集）

---

## 11. モックで確定したこと・残った課題

### 🔵 確定したこと

| # | 項目 | 内容 |
| --- | --- | --- |
| 1 | フロー図のボックス単位 | タスク単位ではなく**手順単位**。既存モックの融合バグを解消（3 章） |
| 2 | フェーズ色 | 状態表示に上書きされない。左バーはフェーズ色のまま（5 章） |
| 3 | 表示条件 | 複合条件（AND）を MVP に含める（3 章・4.4） |
| 4 | 書き出し方式 | viewer の CSS/JS を共有し `textContent` を埋め込む（7 章） |
| 5 | 接続線の実装 | **自前 SVG で R1〜R6 を満たせることを確認。React Flow は採用しない** |
| 6 | 検証・経路一覧 | フェーズ 0 に繰り上げ（10 章） |
| 7 | 試走の進捗 | 保存しない。編集の下書きを汚さない |

### 接続線について（6 章の未決を解消）

既存ロジックを移植し、モックで 5 つの事象すべてを描画して確認した。

- R1〜R5 は満たせた。R6（後戻り本数の警告）も実装済み。
- ダミーデータでの後戻り本数は 0〜3 本。しきい値 5 本には届かず、下部迂回路は混まなかった。
- **ただし実フローは未検証。** 実際の手順書は行き来が多い可能性がある。
  実フローを 1 本移植した時点で、R3（線の重なり回避）を再評価する。

### 残った課題

| # | 論点 | 状態 |
| --- | --- | --- |
| 1 | 詳細フィールドのリッチテキスト形式（HTML 直書き / Markdown / 制限付きエディタ） | 未決。モックは生テキスト入力 |
| 2 | 手順の詳細に画像を貼れるようにするか | 未決 |
| 3 | フロントエンドのビルド・配布方法（開発サーバのみ / FastAPI から静的配信） | 未決 |
| 4 | 実データの投入方針（個人名・IP・ルール名の扱い） | 未決（9 章） |
| 5 | 実フローでの接続線の破綻有無 | 実フロー移植時に検証 |
| 6 | フェーズ自体の編集 UI | モック未実装。データ構造は対応済み |

### モックでできること・できないこと

| | 状態 |
| --- | --- |
| 事象の作成・複製・削除 | ○ |
| タスクのパレットからのドラッグ投入 / ＋ボタン追加 | ○ |
| タスクの新規作成 | ○（`prompt` による簡易入力） |
| 手順の内容編集（タイトル・詳細・SLA・エスカレ） | ○ |
| 判断ステップの設定・選択肢の増減 | ○ |
| 表示条件の設定（複合・AND） | ○ |
| 手順レーンでの並べ替え | ○ |
| 検証・経路一覧 | ○ |
| 試走モード | ○ |
| 単一 HTML の書き出し（プレビュー＋ダウンロード） | ○ |
| フェーズの編集 | ✕ |
| タスクの改名・削除 | ✕ |
| JSON 入出力 | ✕ |
| 状態の保存 | localStorage のみ |

---

## 12. 用語（`PROJECT-BRIEF.md` 8 章を継承）

| 用語 | 意味 |
| --- | --- |
| 事象（event） | インシデントの種別。対応フロー 1 本に対応する |
| フェーズ（phase） | フロー図の列。対応の段階 |
| タスク（task） | フロー図のボックス。**複数の事象で再利用される部品** |
| 手順（step） | ある事象における 1 つの作業指示。タスクを参照する |
| 判断ステップ（decision） | 回答が以降の表示を分岐させる手順 |
| 表示条件（step_condition） | その手順を表示する条件 |
| 対象外 | 条件に合わず自動でスキップされた手順 |
