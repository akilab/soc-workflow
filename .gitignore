# エージェントの作業メモ（明示的な依頼があるまでコミットしない）
working-memory/

# Python（uv で作るプロジェクト内環境。モックのビルド道具に使う）
.venv/
__pycache__/
*.py[cod]

# Go のビルド成果物
/soc-workflow
/soc-workflow.exe
/dist/

# 実行時に作られるデータ（種データは internal/store/seed.json が正本）
/soc-workflow.json
/soc-workflow.json.lock
*.json.lock

# フロントエンド
/web/node_modules/
/web/dist/
