# Generate SOC Workflow — プロジェクト概要

SOC のインシデント対応フローを、対応者が迷わず辿れる形で提供する。
既存の Excel 手順書を置き換えることが目的。

作成日: 2026-09-01
状態: **モック完成、実データ未投入**

---

## 1. 背景と課題

新サービス移行後、SOC の対応フローが複雑化し、Excel の手順書が使いにくくなっている。

### 現状の具体的な問題

| 問題 | 何が起きているか |
| --- | --- |
| あちこちに飛ぶ | 別シートへの参照が多く、対応者が全体像を見失う |
| **条件分岐で迷子になる** | 「〇〇だったらここは飛ばして□□へ」という記述を、対応者が毎回自分で解決している |
| 進捗が分からない | どこまで実施したかが記録されず、引き継ぎ時に不明になる |
| 記録が残らない | 実施内容を別途手入力しており、報告書作成に時間がかかる |
| 注意事項が読まれない | 重要な注記が別セル・別シートにあり、実施の瞬間に目に入らない |

### 解決の方針

**「対応者が探す」作業をゼロにする。**

- 全体を 1 画面に置き、選択した経路だけを浮かび上がらせる
- 条件分岐は「対応者が飛び先を探す」のではなく「**該当しない手順が自動で消える**」
- 注記は該当手順の本文内に置く（別の場所に逃がさない）

---

## 2. 完成しているもの

`mock/soc-flow-mock.html`

単一 HTML ファイル。CSS / JavaScript を内包し、外部依存はゼロ。
ブラウザで開くだけで動作する。サーバー不要、ネットワーク接続不要。

### 画面構成

```
┌──────────────────────────────────────────────────────────┐
│ ヘッダー（固定）: タイトル / 次にやること / 経過時間 / 進捗 │
├────────────┬─────────────────────────────────────────────┤
│ 発生した事象 │ フロー図                                     │
│ （選択リスト）│ 列 = 担当、行 = 手順の順番（→ 下の追記）      │
├────────────┴─────────────────────────────────────────────┤
│ 対応手順（選択した事象の手順が番号付きで並ぶ）              │
└──────────────────────────────────────────────────────────┘
```

### 実装済みの機能

| 機能 | 内容 |
| --- | --- |
| 事象の選択 | 左のリストから選ぶと、フロー図に経路が浮かび上がる |
| **条件分岐の自動解決** | 判断ステップに答えると、条件に合わない手順が自動でスキップ表示になる |
| 現在ステップの明示 | ヘッダー・フロー図・手順リストの 3 か所で同時に示す |
| 進捗管理 | チェックボックス、進捗バー、完了数の表示 |
| 経過時間 | 事象選択時から計測。SLA バッジと並べて確認できる |
| 作業ログの出力 | クリップボードにコピー。スキップした手順も理由付きで残る |
| 状態の保持 | localStorage に保存。ブラウザを閉じても再開できる |

### 動作確認済みの挙動

「EDR がマルウェアを検知」フローで検証：

- 判断前は 14 手順すべて表示
- 「誤検知」と回答 → **6 手順が自動で消え、8 手順になる**
- 「真陽性」と回答 → 誤検知専用の手順だけ消え、13 手順になる
- 手順を完了させると、フロー図のボックスと接続線が緑に変わる

---

## 3. データ構造

ファイル冒頭の 3 つの配列を差し替えれば、内容が入れ替わる。
**画面のロジックには手を入れずに、別のフロー集に転用できる。**

### PHASES — フロー図の列

```javascript
const PHASES = [
  {id:"recv", name:"受信・確認", color:"var(--c1)"},
  ...
];
```

現在は 6 列（受信・確認 / 情報収集 / 判断 / 封じ込め / 復旧 / 記録・報告）。
**実組織のフェーズ名に合わせて変更する前提**。

> **【2026-09-03 追記】この判断は撤回した。列はフェーズではなく担当にした。**
>
> フェーズを列にすると、受信の直後に報告へ飛ぶようなフローで線が端から端まで伸び、
> そこからまた戻ってくる。「段階は時間とともに一方向に進む」という前提が、
> 実際の対応では成り立たなかった。
>
> 実データ 5 事象・接続線 54 本で測ると、フェーズ軸では線が最大 4 列ぶん動くのに対し、
> 担当軸では**全 54 本が隣の列までに収まる**。受け渡しは Tier1 と Tier2、Tier2 と CSIRT の
> ように、隣り合う責任範囲の間でしか起きないため。
>
> あわせて図を縦に伸ばし、**行＝手順の順番**にした。これにより線 i は行 i と行 i+1 の
> あいだの帯しか通らなくなり、**交差が起こり得なくなった**（実測で 0 本）。
> フェーズは色とラベルとして残している。詳細は `docs/SPEC.md` 6 章。

### TASKS — フロー図のボックス

```javascript
T(id, phase, label, note)
// 例: T("iso", "cont", "端末のネットワーク隔離", "EDR から実行")
```

| 引数 | 内容 |
| --- | --- |
| `id` | 一意な識別子。手順から参照する |
| `phase` | 所属する列の id |
| `label` | ボックスに表示する名称 |
| `note` | 補足（小さく表示） |

**タスクは複数の事象から再利用される。** 「端末の隔離」は EDR 検知でもランサムウェアでも使う。

### EVENTS — 事象と手順

```javascript
{
  id:"edr", sev:"S2",
  title:"EDR がマルウェアを検知",
  sub:"端末で不審な実行ファイルが検出された",
  steps:[
    {task:"ack", t:"アラートを受領し担当を確定する", sla:"5分",
     d:"SIEM でチケットを起票し、自分をアサインする。<code>担当者未設定のまま調査を始めない</code>。"},

    {task:"tp", t:"真陽性か誤検知かを判定する", sla:"30分",
     d:"...",
     ask:{key:"tp", label:"判定結果はどちらですか？",
          opts:[{v:"yes", l:"真陽性"}, {v:"no", l:"誤検知"}]}},

    {task:"iso", t:"端末をネットワークから隔離する", when:"tp=yes", esc:1, sla:"15分",
     d:"..."}
  ]
}
```

#### step のキー

| キー | 必須 | 内容 |
| --- | --- | --- |
| `task` | ○ | 対応する TASKS の id。フロー図のどのボックスを光らせるか |
| `t` | ○ | 手順のタイトル |
| `d` | ○ | 手順の詳細。HTML 可。`<code>` で強調できる |
| `sla` | | SLA バッジを表示（例 `"15分"`, `"即時"`） |
| `esc` | | エスカレーション判断が必要な手順に `1` |
| `when` | | **表示条件**。`"キー=値"` 形式 |
| `ask` | | **判断ステップ**。回答が `when` の解決に使われる |

#### 条件分岐の仕組み

これがこのツールの中核。

1. `ask` を持つ手順が判断を尋ねる（`key` に回答が保存される）
2. `when:"tp=yes"` を持つ手順は、`tp` の回答が `yes` のときだけ表示される
3. 回答前は**すべて表示**される（分岐の存在が見える）
4. 回答後は該当しない手順が**取り消し線 + 淡色**になり、進捗の分母からも外れる

**Excel の「〇〇なら□□へ飛ぶ」を、この 2 つのキーだけで表現できる。**
対応者は飛び先を探さない。上から順に消化するだけになる。

---

## 4. 設計判断とその理由

**変更する前に理由を確認すること。** いずれも意図がある。

| 判断 | 理由 |
| --- | --- |
| 単一 HTML・外部依存ゼロ | 配布がファイルコピーだけで済む。SOC 環境はネットワーク制限が厳しいことがある |
| ダークテーマ | SOC は暗い室内・長時間の運用が多い |
| **色だけに頼らない**（▶ / ✓ / — の記号併用） | 色覚特性のある対応者でも判別できるようにする |
| 「現在のステップ」を 3 か所で示す | 焦っている状況で探させない |
| 現在ステップにだけ大きな完了ボタン | 小さいチェックボックスの誤クリックを防ぐ。判断ステップではボタンを出さず、判断を飛ばせないようにする |
| **判断ステップは選択肢のみ**（完了ボタンなし） | 未回答のまま先へ進めない |
| 接続線は「右から出て左から入る」を厳守 | 視線の流れを一定にする。後戻りは丸角の直角配線で回り込ませる |
| 同じ列の隣接ステップは短絡経路 | 1 つ下へ進むだけで画面下まで大回りするのを避ける |
| 進捗は localStorage のみ | 外部送信しない。SOC の作業内容は機密性が高い |
| スキップした手順もログに残す | 監査で「なぜこの手順を実施していないか」に答えられる |

### 接続線のルーティング（触る場合の注意）

`paint()` 内で以下を判定している。

- 前進（次のボックスが右にある）→ ベジエ曲線で直行
- 同じ列で間が空いている → `gapLane()` が返す y を通る短い折返し
- それ以外の後戻り → 全ボックスの下に確保した迂回路（`.grid` の `padding-bottom: 104px` がその領域）

**既知の限界**: 同じ列で連続するステップが多いと折返しが増える。
後戻りが 5 本を超えると下部の迂回路が混む。
**線がごちゃつくのは、フロー自体が行ったり来たりしているサイン**でもあるため、
可視化を直す前にフローの並び順を疑うこと。

---

## 5. 未実装・未検証

| 項目 | 状態 |
| --- | --- |
| 実データ | **未投入**。現在の内容はすべてダミー |
| 判断のネスト | 判断が 2 段以上重なる場合を未検証（`when` は単一条件のみ対応） |
| 複数フローの往来 | 「フィッシング → 不審ログインのフローへ移行」を文章で書いているだけ。リンクしていない |
| 印刷 | 未対応 |
| 複数人での同時対応 | 想定していない（1 人 1 ブラウザ） |
| 担当者・時刻の記録 | 経過時間のみ。誰がいつ実施したかは未記録 |

---

## 6. 次にやること（候補）

### 優先度：高

1. **実フローを 1 本移植する。**
   一番複雑で評判の悪いフローを選ぶ。この形式で捌けるか（判断のネスト、フロー間の往来）が判明する。
2. **列（担当）の名称を実組織に合わせる。**
   既存の用語で読めるようにする。顧客・管理職・外部機関なども列にできる。

### 優先度：中

3. 判断のネスト対応（`when` の複合条件）
4. フロー間リンク（別フローへ移行するステップ）
5. 担当者名の記録（作業ログに含める）

### 検討事項

6. **既存 Excel からのデータ生成。**
   プロジェクト名が示す通り、最終的には手順書を手で書き写すのではなく、
   既存資料から `EVENTS` の JavaScript / JSON を生成したい。
   Claude Agent SDK でファイルを読ませて構造化出力させる案があるが、未着手。
   まずは手作業で 1〜2 本作り、**データ構造が実務に耐えるか**を確認してから判断する。

---

## 7. 制約・注意

- **実運用データを入れた版の取り扱いに注意する。**
  対応フローは「どこを見ていて、どこを見ていないか」の情報を含む。
  社外共有・クラウド保存・スクリーンショットは、通常の資料より慎重に扱う。
- 現在のダミー内容は**実運用の手順ではない**。そのまま使わない。
- 個人名・IP・ホスト名・実際の検知ルール名をデータに含めない方針とするか、
  含めるなら管理方法を決めること。

---

## 8. 用語

| 用語 | 意味 |
| --- | --- |
| 事象（EVENT） | 発生したインシデントの種別。対応フロー 1 本に対応する |
| 担当（LANE） | フロー図の列。誰がやるか。顧客・Tier1・Tier2・CSIRT など |
| フェーズ（PHASE） | 対応の段階。列ではなく、ボックスの色とラベルで表す |
| タスク（TASK） | フロー図のボックス。実施する作業の単位。複数の事象で再利用される |
| 手順（step） | ある事象における 1 つの作業指示。タスクを参照する |
| 判断ステップ | `ask` を持つ手順。回答が以降の表示を分岐させる |
| 対象外 | 条件に合わず自動でスキップされた手順 |
