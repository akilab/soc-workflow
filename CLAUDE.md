@AGENTS.md

# Generate SOC Workflow

SOC のインシデント対応フローを、対応者が迷わず辿れる形で提供するプロジェクト。
既存の Excel 手順書の置き換えが目的。

## 最初に読むもの

- **`PROJECT-BRIEF.md`** — 背景・課題・データ構造・設計判断・次の一手。作業前に必ず読む。
- **`mock/soc-flow-mock.html`** — 動作するモック。ブラウザで開けばそのまま動く。

## このプロジェクト固有のルール

- **モックの設計判断には理由がある。** `PROJECT-BRIEF.md` の「4. 設計判断とその理由」を読まずに
  変更しない。特に以下は意図的なものであり、単純化しない。
  - 単一 HTML・外部依存ゼロ
  - 色だけに頼らない状態表示（▶ / ✓ / —）
  - 接続線の「右から出て左から入る」規則
  - 判断ステップに完了ボタンを出さないこと

- **データとロジックを分離したまま保つ。**
  ファイル冒頭の `PHASES` / `TASKS` / `EVENTS` を差し替えるだけで
  別のフロー集に転用できる状態を維持する。

- **実運用データを扱うときは機密性に注意する。**
  対応フローは「どこを見ていて、どこを見ていないか」の情報を含む。
  ダミーと実データを同じファイルに混ぜない。

- 変更したら**ブラウザで実際に動かして確認する**。
  特に接続線の描画は座標計算なので、目視かプログラムでの検証が必須。

## 技術的な前提

- 単一 HTML ファイル。ビルド不要、依存パッケージなし。
- フォント: Windows は `Meiryo UI`、macOS は `Hiragino Sans`。
- 状態保持は `localStorage` のみ。外部送信は行わない。
